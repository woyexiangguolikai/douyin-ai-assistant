# ???? - ?????? (????? -> ?? -> ?? -> ???? ???)
# ????????? douyin-ai-assistant/admin-web/dist

# ??????
location / {
    root /www/wwwroot/your-domain/admin-web/dist;
    index index.html;
    try_files $uri $uri/ /index.html;
}

# PHP API ??
location /api/ {
    alias /www/wwwroot/your-domain/server/;
    try_files $uri /server/index.php?$query_string;
    
    location ~ \.php$ {
        fastcgi_pass unix:/tmp/php-cgi-74.sock;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $request_filename;
        include fastcgi_params;
    }
}

# ?? .env ????
location ~ \.env {
    deny all;
}
