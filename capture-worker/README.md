# Capture Worker - ????????

?? Playwright ? Node.js ?????PHP ???????????????? Node.js ???

## ??

```bash
cd capture-worker
npm install playwright ws
npx playwright install chromium
```

## ??

?? `.env.example` ? `.env` ????

```
PHP_API_URL=http://localhost:8080/api
DEEPSEEK_API_KEY=your-key-here
ROOM_ID=your-douyin-room-id
```

## ??

```bash
# ??
node worker.js

# ?? (??? PM2)
npm install -g pm2
pm2 start worker.js --name capture-worker
```
